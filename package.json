{
  "name": "anuraos-tadpole",
  "version": "0.1.0",
  "description": "An education environment for anuraOS",
  "private": true,
  "type": "module",
  "scripts": {
    "build-rt": "cd tadpole-rt && esbuild src/index.jsx --bundle --outfile=dist/tadpole-rt.js --target=esnext --format=esm --jsx=transform --jsx-factory=h --jsx-fragment=Fragment --minify"
  },
  "keywords": [],
  "author": "Endercass",
  "license": "MIT",
  "packageManager": "pnpm@10.10.0",
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
